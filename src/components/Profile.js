import React from 'react'
import Navbar from './Navbar';

function Profile() {
  // const currentUser = JSON.parse(localStorage.getItem("currentUser"));
  return (
    <div>
      <Navbar />
      <h2>YOUR PROFILE! HA HA HA </h2>
    </div>
  )
}

export default Profile
